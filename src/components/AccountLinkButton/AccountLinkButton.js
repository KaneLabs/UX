import React from 'react';
// import { AccountButton } from 'eros-ui/components';

const AccountLinkButton = (props) => {
  const { handle } = props;
  return null;
  // <AccountButton onHover={event => console.log('hover')} size={32} />
};

export default AccountLinkButton;
